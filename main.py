# main.py
"""
(РЕФАКТОРИНГ И МИГРАЦИЯ НА MONGO) Главная точка входа приложения.

Обязанности этого файла:
1. Настроить логирование (самое первое).
2. (УДАЛЕНО) Вызов настройки таблиц БД.
3. Импортировать 'app' и 'run_api_server' из 'api.router'.
4. Запустить API-сервер.
"""

# --- 1. Настройка логирования (ДО ВСЕХ ИМПОРТОВ) ---
from dotenv import load_dotenv
load_dotenv()

import logging

logging.basicConfig(
    level=logging.INFO, 
    format='[%(asctime)s] %(levelname)-8s %(name)-20s | %(message)s',
    handlers=[
        logging.StreamHandler() 
    ]
)
log = logging.getLogger(__name__)
log.info("✅ Логирование настроено (main.py).")
# --- Конец блока логирования ---

# (ИЗМЕНЕНИЕ) Импорт setup_database_tables УДАЛЕН
# from database import setup_database_tables 

# Импортируем 'app', в котором УЖЕ ЕСТЬ
# middleware, startup/shutdown и роутеры с префиксами.
from api.router import app, run_api_server


# --- Основной поток выполнения ---
def main():
    """Главная функция запуска."""
    log.info("="*50)
    log.info("🚀 ЗАПУСК СЕРВИСА COINSIFTER (Архитектура: MongoDB)")
    log.info("="*50)

    try:
        # --- (ИЗМЕНЕНИЕ) Этап 1: Настройка БД (PostgreSQL) УДАЛЕН ---
        # MongoDB настраивается "на лету" при первом вызове 
        # и через 'startup' эвент в 'api/router.py'
        
        # --- (ИЗМЕНЕНИЕ) Этап 2 стал Этапом 1 ---
        log.info("...[Этап 1/1] Запуск API-сервера (FastAPI / Uvicorn)...")
        
        # Запускаем сервер.
        # 'run_api_server' вызовет uvicorn.run(app, ...)
        # 'app' при старте вызовет 'startup' (загрузка кэша Mongo и ЧС)
        run_api_server(host="127.0.0.1", port=8000) 

    except Exception as e:
        log.critical(f"❌ КРИТИЧЕСКАЯ ОШИБКА ПРИ ЗАПУСКЕ: {e}", exc_info=True)
        import sys
        sys.exit(1)

if __name__ == "__main__":
    main()