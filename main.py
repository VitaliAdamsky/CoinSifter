"""
(РЕФАКТОРИНГ) Главная точка входа приложения.

Обязанности этого файла:
1. Настроить логирование (самое первое).
2. Вызвать настройку таблиц БД (database.setup_database_tables).
3. Импортировать 'app' и 'run_api_server' из 'api.router'.
4. Запустить API-сервер.
"""

# --- 1. Настройка логирования (ДО ВСЕХ ИМПОРТОВ) ---
from dotenv import load_dotenv
load_dotenv()

import logging

logging.basicConfig(
    level=logging.INFO, 
    format='[%(asctime)s] %(levelname)-8s %(name)-20s | %(message)s',
    handlers=[
        logging.StreamHandler() 
    ]
)
log = logging.getLogger(__name__)
log.info("✅ Логирование настроено (main.py).")
# --- Конец блока логирования ---

from database import setup_database_tables

# (ИЗМЕНЕНО) Импортируем 'app', в котором УЖЕ ЕСТЬ
# middleware, startup/shutdown и роутеры с префиксами.
from api.router import app, run_api_server

# (ИЗМЕНЕНО) Удалены все импорты из api.endpoints (health, trigger, ...)
# (Эта работа теперь полностью выполняется в 'api/router.py')


# --- Основной поток выполнения ---
def main():
    """Главная функция запуска."""
    log.info("="*50)
    log.info("🚀 ЗАПУСК СЕРВИСА COINSIFTER")
    log.info("="*50)

    try:
        # --- Этап 1: Настройка БД ---
        # (Это тот же вызов, что был у вас в 'lifespan')
        log.info("...[Этап 1/2] Настройка таблиц БД (PostgreSQL)...")
        setup_database_tables()
        log.info("...[Этап 1/2] ✅ Таблицы БД успешно настроены.")

        # --- Этап 2: Запуск API-сервера ---
        log.info("...[Этап 2/2] Запуск API-сервера (FastAPI / Uvicorn)...")
        
        # (ИЗМЕНЕНО) Удален весь блок app.include_router(...)
        # (Эта работа теперь полностью выполняется в 'api/router.py')
        
        # (ИЗМЕНЕНО) Удален @app.get("/")
        # (Он не нужен, /api/v1/health его заменяет)
        
        # Запускаем сервер.
        # 'run_api_server' вызовет uvicorn.run(app, ...)
        # 'app' при старте вызовет 'startup' (прогрев кэша)
        run_api_server(host="127.0.0.1", port=8000) 

    except Exception as e:
        log.critical(f"❌ КРИТИЧЕСКАЯ ОШИБКА ПРИ ЗАПУСКЕ: {e}", exc_info=True)
        import sys
        sys.exit(1)

if __name__ == "__main__":
    main()