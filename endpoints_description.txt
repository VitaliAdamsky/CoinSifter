Понял. Вот структурированная документация без таблиц, в чистом Markdown.

Аутентификация
Большинство эндпоинтов (🔴) защищены. Для доступа к ним необходимо передавать HTTP-заголовок X-Auth-Token с вашим SECRET_TOKEN.

🔴 Защищенный — Требует X-Auth-Token.

🟢 Публичный — Доступен без токена.

🩺 Health (Здоровье)
GET /health 🟢 Публичный

Проверка "жизни" сервера (для Render).

HEAD /health 🟢 Публичный

Аналог GET /health (для Render).

POST /health/cache/reload 🔴 Защищенный

(Новый) Принудительно очищает кэш в памяти и загружает свежие данные из MongoDB.

🚀 Trigger (Запуск)
POST /trigger/run-analysis 🔴 Защищенный

(Главный) Запускает полный, асинхронный процесс анализа монет в фоне.

🪙 Coins (Монеты)
GET /coins/filtered 🔴 Защищенный

(Основной) Возвращает полный JSON-список отфильтрованных монет из кэша.

GET /coins/filtered/csv 🟢 Публичный

Возвращает список монет (из кэша) в формате CSV.

GET /coins/formatted-symbols 🔴 Защищенный

(TradingView) Возвращает JSON-список монет (из кэша), отформатированный для TradingView.

📊 Data Quality (Качество данных)
GET /data-quality-report 🔴 Защищенный

Возвращает JSON-отчет о качестве данных (пропуски, NaN) в кэше.

⛔ Blacklist (Черный список)
GET /blacklist 🔴 Защищенный

Возвращает текущий "черный список" (global_blacklist) из MongoDB.

📜 Logs (Логи)
GET /logs 🔴 Защищенный

Возвращает последние 100 логов запусков (script_run_logs) из MongoDB.

POST /logs/clear 🔴 Защищенный

(Новый) Полностью очищает всю коллекцию script_run_logs в MongoDB.